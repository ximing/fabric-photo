# 开发计划：Phase 3 — 新 demo + CI 切换 + 旧代码清理

日期：2026-01-07

对应技术方案：`docs/design/2025-11-08-monorepo-core.md`

## 目标

用 `@gmi/fp-react` 重写 demo（Vite + React 19），删除旧库代码与旧构建链路，CI 切换到 monorepo 验证 + 部署，readme 重写。线上 `https://ximing.github.io/fabric-photo/` 切换为新 UI。

## 约束

- 线上 URL、`dist-demo` → `gh-pages`、`GITHUB_TOKEN` 部署方式不变
- demo 端口 9876；build `base` 为 `/fabric-photo/`
- demo 经 workspace 消费两包 **dist 产物**（`predev` / `prebuild` 先构建 `@gmi/*`）
- 删除旧代码前必须确认新 demo 已可独立运行
- `scripts/ralph/` 不动

## 任务与落地

| 序号 | 任务 | 落地提交 | 日期 |
| --- | --- | --- | --- |
| 1 | demo 包化为 workspace 成员 | `feat(demo): demo 包化为 workspace 成员（Vite + React 19 + fp-react）` | 2026-01-07 |
| 2 | 编辑器接入与上传入口 | `feat(demo): 编辑器接入与上传入口` | 2026-01-07 |
| 3 | 本地全功能冒烟 | （验证任务，并入 Task 2 验收） | 2026-01-07 |
| 4 | 删除旧 src 与旧构建链路 | `chore: 删除旧库 src 与旧构建链路，root 瘦身为纯 monorepo 枢纽` | 2026-01-07 |
| 5 | CI + readme / CLAUDE.md | `docs+ci: CI 切换 monorepo 验证+部署；readme/CLAUDE.md 重写` | 2026-01-07 |

CI 在部署前跑 `pnpm -r typecheck` 与 `pnpm -r test`。

## 验收

- `pnpm -r typecheck && pnpm -r test && pnpm build:demo` 全绿
- 仓库无旧 `src/`、无 root 旧构建配置
- 线上为新 Figma 式 UI，示例图可加载、绘制可用
