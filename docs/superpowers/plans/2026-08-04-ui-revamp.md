# 编辑器 UI 产品化重构实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把编辑器 UI 重构为明暗双主题的专业工具风：主题 token 系统、浮动工具选项条（消除空白第二排）、缩放胶囊、导出弹层 segmented 化。

**Architecture:** 纯 `packages/react`（+ demo 样式）改造，core 零改动。主题经 CSS 变量（`.fp-editor` 作用域，`data-theme` 覆盖 `prefers-color-scheme`）+ react context 暴露切换；浮动 UI（选项条/缩放胶囊）作为 `CanvasView` children 绝对定位叠在画布区（z-index 穿透到 `fp-editor` 层叠上下文，盖在 `fp-canvas-mount` 之上）。

**Tech Stack:** React 19 + TypeScript strict + vitest（jsdom + @testing-library/react）+ 手写 CSS（`@layer components`，fp- 前缀，无 Tailwind base）。

**Spec:** `docs/superpowers/specs/2026-08-04-ui-revamp-design.md`

## Global Constraints

- 包管理器 pnpm；每个 Task 结尾必须 `pnpm typecheck && pnpm --filter @gmi/fp-react test` 全绿才 commit
- `packages/react` 只允许 import `@gmi/fp-core` 公开 API；不得暴露 fabric 类型；组件零编辑逻辑（操作走 core 公开 API）
- 样式全部手写于 `styles.css` 的 `@layer components`，fp- 前缀；组件不含内联样式
- 测试与源文件同目录平铺（`*.test.tsx`）；jsdom 环境；`afterEach(cleanup)` 需手动调（vitest 未开 globals）
- lucide-react 0.344 与 @types/react 19 类型不兼容：图标一律 `as unknown as JSX.ElementType` 收窄（见现有 top-bar.tsx 模式）
- 提交信息中文、conventional commits 前缀（feat/refactor/test/docs）

---

### Task 1: 主题 tokens 落地 styles.css（变量定义 + 全量色值迁移 + 面板细节）

**Files:**
- Modify: `packages/react/src/styles.css`

**Interfaces:**
- Produces（后续所有 Task 依赖的 token 名，一字不差）:
  `--fp-bg-app` `--fp-bg-panel` `--fp-bg-elevated` `--fp-bg-hover` `--fp-border` `--fp-text` `--fp-text-dim` `--fp-accent` `--fp-accent-soft` `--fp-shadow-pop` `--fp-danger` `--fp-danger-soft` `--fp-warn-bg` `--fp-warn-text`
- Produces（主题挂载约定）: `.fp-editor[data-theme="dark"|"light"]` 覆盖系统偏好；无 `data-theme` 时跟随 `prefers-color-scheme`

- [ ] **Step 1: 在 `.fp-editor` 规则顶部定义亮色 token 默认值 + 暗色两套覆盖**

在 `styles.css` 的 `.fp-editor {` 块最前面插入（替换现有 `background: #ffffff; color: #1e1e1e;` 为 `background: var(--fp-bg-panel); color: var(--fp-text);`）：

```css
    .fp-editor {
        /* ===== 主题 tokens（亮色默认） ===== */
        --fp-bg-app: #f5f5f5;
        --fp-bg-panel: #ffffff;
        --fp-bg-elevated: rgba(255, 255, 255, 0.92);
        --fp-bg-hover: #f0f0f0;
        --fp-border: #e5e5e5;
        --fp-text: #1e1e1e;
        --fp-text-dim: #666666;
        --fp-accent: #0074d9;
        --fp-accent-soft: rgba(0, 116, 217, 0.1);
        --fp-shadow-pop: 0 8px 24px rgba(0, 0, 0, 0.12);
        --fp-danger: #d32f2f;
        --fp-danger-soft: #fdecec;
        --fp-warn-bg: #fff7e0;
        --fp-warn-text: #8a6d1a;
        display: grid;
        /* …其余属性不变… */
    }
    /* 暗色：系统偏好（未被显式 light 覆盖时）与显式 dark 两条路径 */
    @media (prefers-color-scheme: dark) {
        .fp-editor:not([data-theme='light']) {
            --fp-bg-app: #181818;
            --fp-bg-panel: #252526;
            --fp-bg-elevated: rgba(45, 45, 45, 0.92);
            --fp-bg-hover: #3a3a3a;
            --fp-border: #3a3a3a;
            --fp-text: #cccccc;
            --fp-text-dim: #999999;
            --fp-accent: #0d99ff;
            --fp-accent-soft: rgba(13, 153, 255, 0.15);
            --fp-shadow-pop: 0 8px 24px rgba(0, 0, 0, 0.55);
            --fp-danger: #e05555;
            --fp-danger-soft: rgba(224, 85, 85, 0.12);
            --fp-warn-bg: rgba(240, 192, 64, 0.12);
            --fp-warn-text: #d8b13a;
        }
    }
    .fp-editor[data-theme='dark'] {
        --fp-bg-app: #181818;
        --fp-bg-panel: #252526;
        --fp-bg-elevated: rgba(45, 45, 45, 0.92);
        --fp-bg-hover: #3a3a3a;
        --fp-border: #3a3a3a;
        --fp-text: #cccccc;
        --fp-text-dim: #999999;
        --fp-accent: #0d99ff;
        --fp-accent-soft: rgba(13, 153, 255, 0.15);
        --fp-shadow-pop: 0 8px 24px rgba(0, 0, 0, 0.55);
        --fp-danger: #e05555;
        --fp-danger-soft: rgba(224, 85, 85, 0.12);
        --fp-warn-bg: rgba(240, 192, 64, 0.12);
        --fp-warn-text: #d8b13a;
    }
```

注意：`@media` 与 `[data-theme='dark']` 两块值相同但都必须存在（media 查询表达「跟随系统」，`[data-theme]` 表达「手动覆盖」）；暗色两套值重复是 CSS 限制，接受。

- [ ] **Step 2: 全量色值迁移到 var()**

按下表替换 `styles.css` 中所有硬编码颜色（同一选择器块内逐条替换，不改动布局/尺寸属性）：

| 现有值 | 替换为 |
|---|---|
| `#ffffff`（作为面板/按钮/input 背景） | `var(--fp-bg-panel)` |
| `#e5e5e5`（canvas-view 背景） | `var(--fp-bg-app)` |
| `#e5e5e5` / `#e0e0e0` / `#f0f0f0`（边框、分隔线） | `var(--fp-border)` |
| `#f0f0f0` / `#f5f5f5` / `#ececec`（hover 背景） | `var(--fp-bg-hover)` |
| `#1e1e1e` / `#333333`（主文字/图标） | `var(--fp-text)` |
| `#666666` / `#777777` / `#888888` / `#999999`（次要文字） | `var(--fp-text-dim)` |
| `#0d99ff`（accent 文字/边框/`accent-color`） | `var(--fp-accent)` |
| `rgba(13, 153, 255, 0.08)` / `rgba(13, 153, 255, 0.12)`（accent 淡底） | `var(--fp-accent-soft)` |
| `#0b89e8`（export-confirm hover） | 删除该规则（主按钮 hover 不换挡，保持 `var(--fp-accent)`） |
| `#d32f2f` | `var(--fp-danger)` |
| `#f0c0c0`（delete 边框） | `var(--fp-danger)` |
| `#fdecec` | `var(--fp-danger-soft)` |
| `#fff7e0` / `#8a6d1a`（locked hint） | `var(--fp-warn-bg)` / `var(--fp-warn-text)` |
| `#dddddd`（input/toggle 边框） | `var(--fp-border)` |

特例（不迁移，保留字面值）：`.fp-swatch` / `.fp-color-custom` 的 `border: 1px solid rgba(0,0,0,0.25)`（色块自身边框，明暗通用）；`.fp-swatch-active` 的 `0 0 0 2px #ffffff` 内圈改为 `0 0 0 2px var(--fp-bg-panel)`，外圈 `0 0 0 4px #0d99ff` 改 `var(--fp-accent)`；`.fp-export-confirm` 的 `color: #ffffff` 保留（accent 底上的白字明暗通用）。

- [ ] **Step 3: 面板细节刷新（同为 styles.css 内改动）**

1. `.fp-layers-header` 与 `.fp-props-group-label` 追加：

```css
        font-size: 11px;
        text-transform: uppercase;
        letter-spacing: 0.5px;
```

2. 全部按钮统一 focus-visible 描边（在 `.fp-editor button:disabled` 规则后追加）：

```css
    .fp-editor button:focus-visible {
        outline: 2px solid var(--fp-accent);
        outline-offset: 1px;
    }
```

3. `.fp-topbar-btn` / `.fp-option-btn` / `.fp-props-toggle` / `.fp-props-delete` 的 `border-radius` 统一为 `6px`。

- [ ] **Step 4: 验证**

Run: `pnpm typecheck && pnpm --filter @gmi/fp-react test`
Expected: 全绿（纯 CSS 改动，无行为变化）

- [ ] **Step 5: Commit**

```bash
git add packages/react/src/styles.css
git commit -m "feat: 主题 token 系统（CSS 变量明暗双套）+ 全量色值迁移"
```

---

### Task 2: 主题状态与切换（useThemeState + context 扩展 + TopBar 切换按钮 + demo 联动）

**Files:**
- Create: `packages/react/src/theme.ts`
- Modify: `packages/react/src/context.ts`
- Modify: `packages/react/src/provider.tsx`
- Modify: `packages/react/src/top-bar.tsx`
- Modify: `packages/react/src/editor.tsx`
- Modify: `packages/react/src/index.ts`
- Modify: `demo/src/style.css`
- Test: `packages/react/src/theme.test.tsx`（新建）、`packages/react/src/top-bar.test.tsx`、`packages/react/src/editor.test.tsx`

**Interfaces:**
- Produces:
  - `theme.ts`: `export type Theme = 'dark' | 'light'`；`export interface ThemeState { theme: Theme; toggleTheme: () => void }`；`export function useThemeState(): ThemeState`
  - `context.ts`: `EditorUIState` 增加字段 `theme: Theme; toggleTheme: () => void`
  - `provider.tsx`: `EditorProviderProps` 增加可选 `themeState?: ThemeState`
  - `editor.tsx`: 根 div 带 `data-theme={theme}`

- [ ] **Step 1: 写失败测试 `packages/react/src/theme.test.tsx`**

```tsx
import { act, cleanup, render } from '@testing-library/react';
import { afterEach, beforeAll, describe, expect, it } from 'vitest';
import { useThemeState, type ThemeState } from './theme';

beforeAll(() => {
    (globalThis as { IS_REACT_ACT_ENVIRONMENT?: boolean }).IS_REACT_ACT_ENVIRONMENT = true;
});
afterEach(() => {
    cleanup();
    localStorage.clear();
});

function Probe(props: { onState: (s: ThemeState) => void }): null {
    props.onState(useThemeState());
    return null;
}

describe('useThemeState', () => {
    it('jsdom 无 matchMedia 时默认 light；toggle 后 dark 并写 localStorage("fp-theme")', () => {
        let state: ThemeState | null = null;
        render(<Probe onState={(s) => (state = s)} />);
        expect(state!.theme).toBe('light');

        act(() => state!.toggleTheme());
        expect(state!.theme).toBe('dark');
        expect(localStorage.getItem('fp-theme')).toBe('dark');

        act(() => state!.toggleTheme());
        expect(state!.theme).toBe('light');
        expect(localStorage.getItem('fp-theme')).toBe('light');
    });

    it('localStorage 已有 fp-theme=dark 时初值 dark', () => {
        localStorage.setItem('fp-theme', 'dark');
        let state: ThemeState | null = null;
        render(<Probe onState={(s) => (state = s)} />);
        expect(state!.theme).toBe('dark');
    });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `pnpm --filter @gmi/fp-react test -- theme`
Expected: FAIL（`./theme` 不存在）

- [ ] **Step 3: 实现 `packages/react/src/theme.ts`**

```ts
import { useCallback, useState } from 'react';

export type Theme = 'dark' | 'light';

export interface ThemeState {
    theme: Theme;
    toggleTheme: () => void;
}

const STORAGE_KEY = 'fp-theme';

/** jsdom 无 matchMedia：守卫后回退 light（真实浏览器跟随系统偏好）。 */
function systemTheme(): Theme {
    if (typeof window !== 'undefined' && typeof window.matchMedia === 'function') {
        return window.matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light';
    }
    return 'light';
}

/**
 * 主题状态：初值 localStorage("fp-theme") → 系统偏好 → light；
 * toggle 写 localStorage（仅用户显式选择后持久化，未选择前跟随系统）。
 */
export function useThemeState(): ThemeState {
    const [theme, setTheme] = useState<Theme>(() => {
        const saved = typeof localStorage === 'undefined' ? null : localStorage.getItem(STORAGE_KEY);
        return saved === 'dark' || saved === 'light' ? saved : systemTheme();
    });
    const toggleTheme = useCallback(() => {
        setTheme((prev) => {
            const next: Theme = prev === 'dark' ? 'light' : 'dark';
            localStorage.setItem(STORAGE_KEY, next);
            return next;
        });
    }, []);
    return { theme, toggleTheme };
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `pnpm --filter @gmi/fp-react test -- theme`
Expected: PASS

- [ ] **Step 5: 扩展 context / provider / editor 挂载 data-theme**

`context.ts` — `EditorUIState` 改为：

```ts
import type { Theme } from './theme';

/** UI 层状态（core 不存）：工具设置 + 主题。 */
export interface EditorUIState {
    toolSettings: ToolSettings;
    setToolSettings: Dispatch<SetStateAction<ToolSettings>>;
    theme: Theme;
    toggleTheme: () => void;
}
```

`provider.tsx` — 接受可选 themeState（FabricPhotoEditor 注入；直挂测试用内部 fallback）：

```tsx
import { useMemo, type JSX, type ReactNode } from 'react';
import type { Editor } from '@gmi/fp-core';
import { EditorContext, EditorUIContext, type EditorUIState } from './context';
import { useThemeState, type ThemeState } from './theme';
import { DEFAULT_TOOL_SETTINGS, type ToolSettings } from './tool-settings';

export interface EditorProviderProps {
    /** 显式注入（FabricPhotoEditor 或测试创建的无头 Editor）。 */
    editor: Editor;
    /** FabricPhotoEditor 注入（根 div 也要用 theme 挂 data-theme）；缺省内部自建。 */
    themeState?: ThemeState;
    children: ReactNode;
}

/** 提供 Editor 实例、UI 层工具设置与主题；自身不创建 Editor（由调用方持有生命周期）。 */
export function EditorProvider(props: EditorProviderProps): JSX.Element {
    const [toolSettings, setToolSettings] = useState<ToolSettings>(DEFAULT_TOOL_SETTINGS);
    const fallbackTheme = useThemeState();
    const themeState = props.themeState ?? fallbackTheme;
    const uiState = useMemo<EditorUIState>(
        () => ({ toolSettings, setToolSettings, theme: themeState.theme, toggleTheme: themeState.toggleTheme }),
        [toolSettings, themeState]
    );
    return (
        <EditorContext.Provider value={props.editor}>
            <EditorUIContext.Provider value={uiState}>{props.children}</EditorUIContext.Provider>
        </EditorContext.Provider>
    );
}
```

（保留原 `useState` import；上面 import 行仅为示意差异，`useState` 不可删。）

`editor.tsx` — 持有 themeState，根 div 挂 data-theme，注入 provider：

```tsx
// import 增加：import { useThemeState } from './theme';
// FabricPhotoEditor 函数体顶部（containerEl state 之后）：
    const themeState = useThemeState();
// rootClassName 之后 return 的根 div 改：
        <div className={rootClassName} data-theme={themeState.theme}>
// EditorProvider 改：
                <EditorProvider editor={editor} themeState={themeState}>
```

`index.ts` — 追加导出：

```ts
export { useThemeState } from './theme';
export type { Theme, ThemeState } from './theme';
```

- [ ] **Step 6: TopBar 加主题切换按钮 + 失败测试**

先在 `top-bar.test.tsx` 的 `describe('TopBar')` 内追加用例：

```tsx
    it('点击「切换主题」：toggleTheme 被调（经 context），按钮图标随 theme 变化', () => {
        const editor = new Editor();
        const utils = renderWithEditor(editor);

        const toggle = button(utils, '切换主题');
        // 初值 light（jsdom 无 matchMedia）→ 显示 Moon（切到暗色）
        expect(toggle.querySelector('svg')).not.toBeNull();
        act(() => {
            fireEvent.click(toggle);
        });
        expect(localStorage.getItem('fp-theme')).toBe('dark');
        editor.destroy();
        localStorage.clear();
    });
```

`editor.test.tsx` 追加（缺省布局下根 div 拿到 data-theme 并随切换变化）：

```tsx
    it('根 div 带 data-theme；点 TopBar 切换主题后 data-theme 与 localStorage 同步', () => {
        const utils = render(<FabricPhotoEditor />);
        const root = utils.container.querySelector('.fp-editor') as HTMLElement;
        expect(root.dataset.theme).toBe('light');

        act(() => {
            fireEvent.click(utils.getByRole('button', { name: '切换主题' }));
        });
        expect(root.dataset.theme).toBe('dark');
        expect(localStorage.getItem('fp-theme')).toBe('dark');
        localStorage.clear();
    });
```

（editor.test.tsx 现有 import 若无 `act`/`fireEvent` 需补上；该文件已有 render FabricPhotoEditor 的模式可照抄。）

- [ ] **Step 7: 跑测试确认失败**

Run: `pnpm --filter @gmi/fp-react test -- top-bar editor`
Expected: FAIL（没有「切换主题」按钮 / 根 div 无 data-theme）

- [ ] **Step 8: TopBar 实现切换按钮**

`top-bar.tsx`：

```tsx
// import 增加：
import { Moon, Sun } from 'lucide-react';
import { useToolSettings } from './hooks';
const SunIcon = Sun as unknown as JSX.ElementType;
const MoonIcon = Moon as unknown as JSX.ElementType;

// 组件内：
    const { theme, toggleTheme } = useToolSettings();

// JSX：放在「导出」wrapper 之前（fp-topbar-actions 内最右组的开头），并补一条分隔线：
                <button
                    type="button"
                    className="fp-topbar-btn"
                    title="切换主题"
                    aria-label="切换主题"
                    onClick={toggleTheme}
                >
                    {theme === 'dark' ? <SunIcon size={18} aria-hidden /> : <MoonIcon size={18} aria-hidden />}
                </button>
                <span className="fp-topbar-separator" aria-hidden />
```

- [ ] **Step 9: 跑测试确认通过 + 全量回归**

Run: `pnpm typecheck && pnpm --filter @gmi/fp-react test`
Expected: 全绿

- [ ] **Step 10: demo 联动**

`demo/src/style.css` 追加（页面底色跟随系统；编辑器区自身主题由 `.fp-editor` 变量承载）：

```css
body {
    background: #f5f5f5;
}
@media (prefers-color-scheme: dark) {
    body {
        background: #181818;
    }
}
```

`.demo-upload` 的 `right: 248px` 保持不变（TopBar 右侧仍有导出组）；颜色字面值不动（demo 层样式）。

- [ ] **Step 11: Commit**

```bash
git add packages/react/src/theme.ts packages/react/src/theme.test.tsx packages/react/src/context.ts packages/react/src/provider.tsx packages/react/src/top-bar.tsx packages/react/src/top-bar.test.tsx packages/react/src/editor.tsx packages/react/src/editor.test.tsx packages/react/src/index.ts demo/src/style.css
git commit -m "feat: 主题状态与切换（useThemeState + data-theme + TopBar 日/月按钮）"
```

---

### Task 3: FloatingOptions —— 布局两行化 + 选项条浮动化

**Files:**
- Rename: `packages/react/src/tool-option-bar.tsx` → `packages/react/src/floating-options.tsx`
- Modify: `packages/react/src/canvas-view.tsx`
- Modify: `packages/react/src/editor.tsx`
- Modify: `packages/react/src/styles.css`
- Modify: `packages/react/src/index.ts`
- Modify: `packages/react/README.md`
- Test: `packages/react/src/floating-options.test.tsx`（由 toolbar.test.tsx 的 `describe('ToolOptionBar')` 迁出改造）、`packages/react/src/toolbar.test.tsx`、`packages/react/src/editor.test.tsx`

**Interfaces:**
- Consumes: Task 1 的 `--fp-bg-elevated` `--fp-border` `--fp-shadow-pop`；Task 2 无交集
- Produces:
  - `floating-options.tsx`: `export function FloatingOptions(props: { className?: string }): JSX.Element | null`，根 class `fp-floating-options`
  - `canvas-view.tsx`: `CanvasViewProps` 增加 `children?: ReactNode`；子元素渲染在 `.fp-canvas-view` div 内（该 div 新增 `position: relative`）
  - `index.ts`: `export { FloatingOptions } from './floating-options';`（**删除** `ToolOptionBar` 导出）

- [ ] **Step 1: 迁移并改造测试**

把 `toolbar.test.tsx` 中 `describe('ToolOptionBar')` 整块剪到新文件 `floating-options.test.tsx`（补齐 import：render/act/cleanup/fireEvent、EditorProvider、FloatingOptions、core 的 Editor/AddObject 等——照搬原块顶部依赖），并做两处改造：

1. 组件名 `ToolOptionBar` → `FloatingOptions`；类名断言 `.fp-option-bar` → `.fp-floating-options`
2. 「normal 模式容器占位但不渲染选项按钮」用例改为「normal 模式返回 null（容器不存在）」：

```tsx
    it('normal/text/pan 模式渲染 null（无空容器）；切到 freedraw 出现浮动条', () => {
        const editor = new Editor();
        const utils = render(
            <EditorProvider editor={editor}>
                <FloatingOptions />
            </EditorProvider>
        );
        expect(utils.container.querySelector('.fp-floating-options')).toBeNull();

        act(() => {
            editor.startFreeDrawing({ width: 4, color: '#ff0000' });
        });
        expect(utils.container.querySelector('.fp-floating-options')).not.toBeNull();

        act(() => {
            editor.endAll();
        });
        expect(utils.container.querySelector('.fp-floating-options')).toBeNull();
        editor.destroy();
    });
```

`toolbar.test.tsx` 中删除已迁出的 describe（保留 Toolbar 三个用例）。

`editor.test.tsx` 的「缺省 children」用例改为断言 `.fp-topbar`、`.fp-toolbar`、`.fp-canvas-view` 存在且 `.fp-option-bar` 不存在（normal 模式下浮动条本就不渲染）：

```tsx
    it('缺省 children 渲染 TopBar + Toolbar + CanvasView（无固定选项行）', () => {
        const utils = render(<FabricPhotoEditor />);
        expect(utils.container.querySelector('.fp-topbar')).not.toBeNull();
        expect(utils.container.querySelector('.fp-toolbar')).not.toBeNull();
        expect(utils.container.querySelector('.fp-canvas-view')).not.toBeNull();
        expect(utils.container.querySelector('.fp-option-bar')).toBeNull();
    });
```

- [ ] **Step 2: 跑测试确认失败**

Run: `pnpm --filter @gmi/fp-react test -- floating-options`
Expected: FAIL（`./floating-options` 不存在）

- [ ] **Step 3: 组件重命名 + null 渲染**

`git mv packages/react/src/tool-option-bar.tsx packages/react/src/floating-options.tsx`，然后改：

1. 组件名 `ToolOptionBar` → `FloatingOptions`，返回类型 `JSX.Element | null`
2. 末尾渲染逻辑改为空内容返回 null：

```tsx
    if (content === null) {
        return null;
    }
    const rootClassName =
        props.className === undefined ? 'fp-floating-options' : `fp-floating-options ${props.className}`;
    return <div className={rootClassName}>{content}</div>;
```

3. 文件头注释更新：浮动条定位在画布区顶部居中，仅 crop/brush/shape/mosaic 渲染。

- [ ] **Step 4: CanvasView 支持 children + editor.tsx 缺省布局调整**

`canvas-view.tsx`：

```tsx
export interface CanvasViewProps {
    editor: Editor;
    className?: string;
    children?: ReactNode; // 浮动 UI（FloatingOptions / ZoomControls），绝对定位叠加在画布区
}
// import 增加 type ReactNode；return 改：
    return (
        <div ref={ref} className={cls}>
            {props.children}
        </div>
    );
```

`editor.tsx` 缺省 children 改为：

```tsx
                    {children ?? (
                        <>
                            <TopBar />
                            <Toolbar />
                            <CanvasView editor={editor}>
                                <FloatingOptions />
                            </CanvasView>
                            <div className="fp-side-panel">
                                <LayersPanel />
                                <PropertiesPanel />
                            </div>
                        </>
                    )}
```

（`import { ToolOptionBar }` 改 `import { FloatingOptions } from './floating-options';`）

- [ ] **Step 5: styles.css 布局两行化 + 浮动条样式**

1. `.fp-editor` 的 grid 改：

```css
        grid-template-areas:
            'top top top'
            'tools canvas props';
        grid-template-rows: 48px 1fr;
```

2. 删除整个 `/* ===== 工具选项条（auto 高，grid 行 2） ===== */` 段中的 `.fp-option-bar` 规则（`.fp-option-group` / `.fp-option-btn` / `.fp-option-btn-active` 保留），替换为：

```css
    /* ===== 浮动工具选项条（画布区顶部居中，仅绘制/crop 模式渲染） ===== */
    .fp-floating-options {
        position: absolute;
        top: 10px;
        left: 50%;
        transform: translateX(-50%);
        z-index: 10;
        display: flex;
        align-items: center;
        gap: 8px;
        padding: 6px 10px;
        border-radius: 10px;
        background: var(--fp-bg-elevated);
        border: 1px solid var(--fp-border);
        box-shadow: var(--fp-shadow-pop);
        backdrop-filter: blur(12px);
    }
```

3. `.fp-canvas-view` 追加 `position: relative;`（作为浮动子元素的定位上下文；浮动条 z-index:10 参与 `.fp-editor` 层叠上下文，盖在同区后置的 `.fp-canvas-mount` 之上）。

- [ ] **Step 6: index.ts 与 README**

`index.ts`：`export { ToolOptionBar } from './tool-option-bar';` 替换为 `export { FloatingOptions } from './floating-options';`

`packages/react/README.md` 组件清单：`ToolOptionBar` 条目改为 `FloatingOptions`（描述：画布顶部浮动工具选项条，仅绘制/crop 模式出现）；缺省布局描述同步。

- [ ] **Step 7: 全量回归**

Run: `pnpm typecheck && pnpm --filter @gmi/fp-react test`
Expected: 全绿

- [ ] **Step 8: Commit**

```bash
git add packages/react
git commit -m "feat: 布局两行化 + ToolOptionBar 重构为画布顶部浮动条 FloatingOptions"
```

---

### Task 4: ZoomControls 浮动胶囊（缩放控件迁出 TopBar）

**Files:**
- Create: `packages/react/src/zoom-controls.tsx`
- Modify: `packages/react/src/top-bar.tsx`
- Modify: `packages/react/src/editor.tsx`
- Modify: `packages/react/src/styles.css`
- Modify: `packages/react/src/index.ts`
- Modify: `packages/react/README.md`
- Test: `packages/react/src/zoom-controls.test.tsx`（新建，迁自 top-bar.test.tsx）、`packages/react/src/top-bar.test.tsx`

**Interfaces:**
- Consumes: Task 3 的 `CanvasView` children 机制；Task 1 tokens
- Produces: `zoom-controls.tsx`: `export function ZoomControls(props: { className?: string }): JSX.Element`，根 class `fp-zoom-controls`；`index.ts` 导出 `ZoomControls`

- [ ] **Step 1: 迁移测试**

新建 `zoom-controls.test.tsx`，从 `top-bar.test.tsx` 剪入三个用例（「点击放大 → setZoom 约 1.2」「点击缩小 → 约 0.8」「点击百分比复位 setZoom(1)」），`renderWithEditor` 内组件换成 `<ZoomControls />`（helper 复制一份到本文件，TopBar 改 ZoomControls）。`top-bar.test.tsx` 删除这三条及不再使用的 `setZoom` spy import 残留。

- [ ] **Step 2: 跑测试确认失败**

Run: `pnpm --filter @gmi/fp-react test -- zoom-controls`
Expected: FAIL（组件不存在）

- [ ] **Step 3: 实现 `zoom-controls.tsx`**

```tsx
import type { JSX } from 'react';
import { Minus, Plus } from 'lucide-react';
import { useEditor, useEditorState } from './hooks';

/** 单次点击的缩放步长（乘区以 0.2 为档）。 */
const ZOOM_STEP = 0.2;

// lucide-react 0.344 的类型基于 React 18 JSX 命名空间，与 @types/react 19
// 的 ReactNode 不兼容；运行时是标准 FC，渲染处收窄为 JSX.ElementType
const MinusIcon = Minus as unknown as JSX.ElementType;
const PlusIcon = Plus as unknown as JSX.ElementType;

/**
 * 缩放控件：画布区底部居中的浮动胶囊（− / 百分比复位 / ＋）。
 * 绝对定位叠加在 CanvasView 内（z-index 盖在 fabric 挂载层之上），不占布局流。
 */
export function ZoomControls(props: { className?: string }): JSX.Element {
    const editor = useEditor();
    const zoomText = useEditorState((state) => `${Math.round(state.viewport.zoom * 100)}%`);

    const rootClassName =
        props.className === undefined ? 'fp-zoom-controls' : `fp-zoom-controls ${props.className}`;
    return (
        <div className={rootClassName}>
            <button
                type="button"
                className="fp-zoom-btn"
                title="缩小"
                aria-label="缩小"
                onClick={() => editor.setZoom(editor.getZoom() - ZOOM_STEP)}
            >
                <MinusIcon size={16} aria-hidden />
            </button>
            <button
                type="button"
                className="fp-zoom-value"
                title="重置缩放"
                aria-label="重置缩放"
                onClick={() => editor.setZoom(1)}
            >
                {zoomText}
            </button>
            <button
                type="button"
                className="fp-zoom-btn"
                title="放大"
                aria-label="放大"
                onClick={() => editor.setZoom(editor.getZoom() + ZOOM_STEP)}
            >
                <PlusIcon size={16} aria-hidden />
            </button>
        </div>
    );
}
```

- [ ] **Step 4: TopBar 移除缩放三件套**

`top-bar.tsx` 删除：`ZOOM_STEP` 常量、`Minus`/`Plus` import 及对应 Icon 收窄、`zoomText` state、JSX 中缩小/重置/放大三个按钮与其后的 `fp-topbar-separator`（保留主题切换按钮后的分隔线）。

- [ ] **Step 5: editor.tsx 挂入 + 样式**

`editor.tsx` 缺省 children 的 CanvasView 内加 `<ZoomControls />`（FloatingOptions 之后）：

```tsx
                            <CanvasView editor={editor}>
                                <FloatingOptions />
                                <ZoomControls />
                            </CanvasView>
```

`styles.css`：删除 `.fp-topbar-zoom-value` 两条规则；追加：

```css
    /* ===== 缩放胶囊（画布区底部居中） ===== */
    .fp-zoom-controls {
        position: absolute;
        bottom: 10px;
        left: 50%;
        transform: translateX(-50%);
        z-index: 10;
        display: flex;
        align-items: center;
        gap: 2px;
        padding: 3px 6px;
        border-radius: 999px;
        background: var(--fp-bg-elevated);
        border: 1px solid var(--fp-border);
        box-shadow: var(--fp-shadow-pop);
        backdrop-filter: blur(12px);
    }
    .fp-zoom-btn {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        width: 24px;
        height: 24px;
        border-radius: 999px;
        color: var(--fp-text);
    }
    .fp-zoom-btn:hover {
        background: var(--fp-bg-hover);
    }
    .fp-zoom-value {
        min-width: 44px;
        height: 24px;
        padding: 0 4px;
        border-radius: 999px;
        font-size: 12px;
        text-align: center;
        color: var(--fp-text);
    }
    .fp-zoom-value:hover {
        background: var(--fp-bg-hover);
    }
```

`index.ts` 追加 `export { ZoomControls } from './zoom-controls';`；README 组件清单加 `ZoomControls`，TopBar 条目描述去掉缩放。

- [ ] **Step 6: 全量回归**

Run: `pnpm typecheck && pnpm --filter @gmi/fp-react test`
Expected: 全绿

- [ ] **Step 7: Commit**

```bash
git add packages/react
git commit -m "feat: 缩放控件迁出 TopBar，画布底部浮动胶囊 ZoomControls"
```

---

### Task 5: 导出弹层 segmented 化

**Files:**
- Modify: `packages/react/src/top-bar.tsx`
- Modify: `packages/react/src/styles.css`
- Test: `packages/react/src/top-bar.test.tsx`

**Interfaces:**
- Consumes: Task 1 tokens；Task 2 的 TopBar 现状
- Produces: segmented 控件类名 `fp-seg` / `fp-seg-btn` / `fp-seg-btn-active`（仅本 Task 使用）；导出行为与 `toDataURL` 参数组装不变

- [ ] **Step 1: 改造测试断言（radio input → 分段按钮）**

`top-bar.test.tsx` 导出相关用例按以下对应关系改（交互路径与参数断言不变）：

1. 「格式切 JPEG/WebP 显示质量滑杆」：`getByRole('radio', { name: 'JPEG' })` 等保留可用（分段按钮用 `role="radio"`），但断言补充选中态：

```tsx
        const jpeg = utils.getByRole('radio', { name: 'JPEG' });
        fireEvent.click(jpeg);
        expect(jpeg.getAttribute('aria-checked')).toBe('true');
        expect(utils.getByRole('radio', { name: 'PNG' }).getAttribute('aria-checked')).toBe('false');
```

2. 「无选中时『仅选中』禁用」：`scopeRadio()` 返回类型改 `HTMLButtonElement`，`disabled` 断言不变（button 同样支持）。
3. 两条确认导出用例：点击目标从 radio input 换成同名分段按钮即可（`fireEvent.click(getByRole('radio', { name: '2x' }))` 不变），`toDataURL` 参数与文件名断言原样保留。

- [ ] **Step 2: 跑测试确认失败**

Run: `pnpm --filter @gmi/fp-react test -- top-bar`
Expected: FAIL（现状 radio input 无 `aria-checked` 属性）

- [ ] **Step 3: 重构导出弹层 JSX**

`top-bar.tsx` 导出弹层改为（state、handleExport、开合逻辑全部不动）：

```tsx
                    {exportOpen && (
                        <div className="fp-export-panel" role="dialog" aria-label="导出设置">
                            <span className="fp-export-label">格式</span>
                            <div className="fp-seg" role="radiogroup" aria-label="格式">
                                {(['png', 'jpeg', 'webp'] as const).map((f) => (
                                    <button
                                        key={f}
                                        type="button"
                                        role="radio"
                                        aria-checked={format === f}
                                        className={
                                            format === f ? 'fp-seg-btn fp-seg-btn-active' : 'fp-seg-btn'
                                        }
                                        onClick={() => setFormat(f)}
                                    >
                                        {f === 'jpeg' ? 'JPEG' : f.toUpperCase()}
                                    </button>
                                ))}
                            </div>
                            {format !== 'png' && (
                                <>
                                    <span className="fp-export-label">质量</span>
                                    <div className="fp-export-row">
                                        <input
                                            type="range"
                                            aria-label="质量"
                                            min={QUALITY_MIN}
                                            max={QUALITY_MAX}
                                            step={QUALITY_STEP}
                                            value={quality}
                                            onChange={(event) => setQuality(Number(event.target.value))}
                                        />
                                        <span className="fp-export-value">{quality.toFixed(2)}</span>
                                    </div>
                                </>
                            )}
                            <span className="fp-export-label">倍率</span>
                            <div className="fp-seg" role="radiogroup" aria-label="倍率">
                                {MULTIPLIERS.map((m) => (
                                    <button
                                        key={m}
                                        type="button"
                                        role="radio"
                                        aria-checked={multiplier === m}
                                        className={m === multiplier ? 'fp-seg-btn fp-seg-btn-active' : 'fp-seg-btn'}
                                        onClick={() => setMultiplier(m)}
                                    >
                                        {m}x
                                    </button>
                                ))}
                            </div>
                            <span className="fp-export-label">范围</span>
                            <div className="fp-seg" role="radiogroup" aria-label="范围">
                                <button
                                    type="button"
                                    role="radio"
                                    aria-checked={scope === 'full'}
                                    className={scope === 'full' ? 'fp-seg-btn fp-seg-btn-active' : 'fp-seg-btn'}
                                    onClick={() => setScope('full')}
                                >
                                    整图
                                </button>
                                <button
                                    type="button"
                                    role="radio"
                                    aria-checked={scope === 'selection'}
                                    disabled={!hasSelection}
                                    className={
                                        scope === 'selection' ? 'fp-seg-btn fp-seg-btn-active' : 'fp-seg-btn'
                                    }
                                    onClick={() => setScope('selection')}
                                >
                                    仅选中
                                </button>
                            </div>
                            <button type="button" className="fp-export-confirm" onClick={handleExport}>
                                导出
                            </button>
                        </div>
                    )}
```

- [ ] **Step 4: 样式重构**

`styles.css`：删除 `.fp-export-choice` / `.fp-export-choice:has(...)` / `.fp-export-row input[type='range']`（flex 规则保留 `.fp-export-row` 本体）等旧规则；`.fp-export-panel` 改：

```css
    .fp-export-panel {
        position: absolute;
        top: calc(100% + 6px);
        right: 0;
        z-index: 20;
        display: flex;
        flex-direction: column;
        gap: 6px;
        width: 220px;
        padding: 12px;
        background: var(--fp-bg-elevated);
        border: 1px solid var(--fp-border);
        border-radius: 10px;
        box-shadow: var(--fp-shadow-pop);
        backdrop-filter: blur(12px);
        font-size: 12px;
        color: var(--fp-text);
    }
    .fp-export-label {
        font-size: 11px;
        text-transform: uppercase;
        letter-spacing: 0.5px;
        color: var(--fp-text-dim);
    }
    /* segmented 控件：底槽 + 等宽分段，选中段 accent 底 */
    .fp-seg {
        display: flex;
        gap: 2px;
        padding: 2px;
        border-radius: 6px;
        background: var(--fp-bg-app);
    }
    .fp-seg-btn {
        flex: 1 1 0;
        height: 24px;
        border-radius: 4px;
        font-size: 12px;
        color: var(--fp-text-dim);
    }
    .fp-seg-btn:hover:not(:disabled) {
        color: var(--fp-text);
    }
    .fp-seg-btn:disabled {
        opacity: 0.5;
    }
    .fp-seg-btn-active,
    .fp-seg-btn-active:hover {
        background: var(--fp-accent);
        color: #ffffff;
    }
```

`.fp-export-row` 保留（质量滑杆行用）；`.fp-export-confirm` 的 `background` 改 `var(--fp-accent)`、`border-radius: 6px`。

- [ ] **Step 5: 全量回归**

Run: `pnpm typecheck && pnpm --filter @gmi/fp-react test`
Expected: 全绿

- [ ] **Step 6: Commit**

```bash
git add packages/react/src/top-bar.tsx packages/react/src/top-bar.test.tsx packages/react/src/styles.css
git commit -m "feat: 导出弹层 segmented 化（去表单感，选项不减少）"
```

---

### Task 6: 真实浏览器验证 + 收尾

**Files:**
- 可能微调: `packages/react/src/styles.css`（目检后色值/间距微调）

- [ ] **Step 1: 全量门槛**

Run: `pnpm typecheck && pnpm test && pnpm build && pnpm build:demo`
Expected: 全绿

- [ ] **Step 2: 启动 dev 并用 kimi-webbridge 截图目检**

`pnpm dev`（端口 9876），webbridge 打开 http://localhost:9876 ，按 webbridge-fabric-canvas-verification 记忆的约定（截图显白是 GPU 伪影，以像素/DOM 为准）验证：

1. 暗色主题（点 TopBar 切换按钮）：顶栏/工具栏/侧栏深色、浮动条玻璃态、缩放胶囊、导出弹层 segmented
2. 亮色主题：同组件清单
3. 画笔模式：浮动条出现且线宽/色板可用；回选择模式浮动条消失（无空行）
4. crop 模式：Apply/Cancel 在浮动条内
5. 导出弹层：格式/倍率/范围分段切换、JPEG 质量滑杆、确认下载

- [ ] **Step 3: 目检问题微调并回归**

如色值/对比度/间距有问题，改 `styles.css` 后重跑 Step 1 门槛。

- [ ] **Step 4: 最终 Commit（如有微调）**

```bash
git add -A
git commit -m "fix: UI 重构浏览器目检微调"
```

---

## Self-Review 记录

- **Spec 覆盖**：tokens（T1）、主题切换（T2）、布局两行 + 浮动条（T3）、缩放胶囊（T4）、导出 segmented（T5）、面板细节（T1 Step 3）、README（T3/T4）、浏览器验证（T6）——spec 六节全覆盖
- **类型一致性**：`ThemeState` / `useThemeState` / `FloatingOptions` / `ZoomControls` / `fp-seg-btn-active` 在 Produces 与 Consumes 间逐字一致；`CanvasViewProps.children` 先产于 T3、消费于 T4
- **已知取舍**：暗色 token 值在 media 查询与 `[data-theme='dark']` 两处重复（CSS 无变量引用变量的便捷路径，接受）；demo 页面底色仅跟随系统（手动切换只作用编辑器区，`.fp-editor` 之外的宿主页面不污染）
